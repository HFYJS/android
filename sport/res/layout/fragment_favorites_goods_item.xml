<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >

    <ImageView
        android:id="@+id/iv_favorites_goods_item_image"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_marginLeft="20dp"
        android:layout_marginTop="52dp"
        android:src="@drawable/ic_launcher" />

    <TextView
        android:id="@+id/tv_favorites_goods_item_sales"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/tv_favorites_goods_item_price"
        android:layout_alignBottom="@+id/tv_favorites_goods_item_price"
        android:layout_marginLeft="33dp"
        android:layout_toRightOf="@+id/tv_favorites_goods_item_price"
        android:text="sales"
        android:textColor="@drawable/light" />

    <TextView
        android:id="@+id/tv_favorites_goods_item_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/iv_favorites_goods_item_image"
        android:layout_marginTop="57dp"
        android:layout_toRightOf="@+id/iv_favorites_goods_item_image"
        android:text="name" />

    <TextView
        android:id="@+id/tv_favorites_goods_item_price"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tv_favorites_goods_item_name"
        android:layout_marginTop="26dp"
        android:layout_toRightOf="@+id/iv_favorites_goods_item_image"
        android:text="price"
        android:textColor="@drawable/light" />

</RelativeLayout>